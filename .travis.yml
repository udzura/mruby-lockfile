dist: trusty
language: c
compiler:
  - gcc
  - clang
env:
  - MRUBY_VERSION=master
  #- MRUBY_VERSION=1.3.0
# matrix:
#   allow_failures:
#     - env: MRUBY_VERSION=master
branches:
  only:
    - master
addons:
  apt:
    packages:
      - rake
      - bison
      - git
      - gperf

script:
  - rake test
